//
// Created by PC on 12/17/2023.
//

#include "NpPassengerQueue.h"
#include "Bus.h"
void NpPassengerQueue::removePassenger(int id) {
//    list<Passenger>& passengers = Queue<Passenger>::getList();
//    list<Passenger>::iterator it = passengers.begin();
//    while (it != passengers.end() && it->getID() != id) {
//        it++;
//    }
//    if (it != passengers.end()) {
//        passengers.erase(it);
//    }
}
